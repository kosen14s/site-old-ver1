machine:
    timezone: Asia/Tokyo
    node:
      version: 5.4.1
deployment:
  production:
    branch: develop
    commands:
      - git config --global user.name "yamasy1549"
      - git config --global user.email "sanae@yamasy.info"
      - gulp deploy
